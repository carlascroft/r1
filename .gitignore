# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# editors
.vscode/
.idea/
*.swp

# local scratch — anything you want on disk but not deployed
scratch/
*.local.html

# secrets (worker tokens, API keys) — never commit these
.env
.dev.vars
